var coordinatePlugin = function (schema, options) {
  var field = {
    x: { type: Number, default: 0 },
    y: { type: Number, default: 0 },
    distance: {type: Number, default: 0},
    weight: {type: Number, default: 0}
  }
  schema.add(field)
}

module.exports = coordinatePlugin
